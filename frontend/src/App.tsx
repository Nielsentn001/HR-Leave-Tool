// Placeholder shell for now. In later steps this will compose the real
// page out of components from src/components/: the 30-day leave grid, the
// pending requests table, and the leave request form.
function App() {
  return (
    <div className="min-h-screen bg-gray-50 p-8">
      <h1 className="text-2xl font-semibold text-gray-800">Leave Management</h1>
      <p className="text-gray-500 mt-1">Scaffold is running. Components come in a later step.</p>
    </div>
  );
}

export default App;
